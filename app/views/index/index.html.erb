<ul>
    <%= content_tag_for(:li, @screening_movies) do |movie| %>
         <%= image_tag movie.image, alt: movie.title %>
         <%= movie.title %>
         <%= link_to "选座购票", movie_path(movie_path) %>
    <% end %>
</ul>
